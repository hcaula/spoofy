# spoofy (name to change)

run after clone `git submodule update --init --recursive`
or clone with `git clone --recurse-submodules -j8 git_url`

after clone build with `docker-compose build`
and rund with `docker-compose up`
